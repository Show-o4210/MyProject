# PVZH Mod 工具箱 — 项目分析与改进建议

> **分析基准**：Render 免费 Web Service 套餐（2026）  
> **分析日期**：2026-06-30  
> **项目路径**：`MyProject/`  
> **线上地址**：https://pvz-h-tools.onrender.com/

---

## 一、执行摘要

本项目是一个基于 **Flask + Supabase + UnityPy** 的植物大战僵尸英雄（PVZH）Mod 工具站，采用 Blueprint 模块化架构，功能覆盖卡组编辑、Unity AB 解包/回填、关卡编辑器、幻影卡牌工坊、卡包购买、卡牌发送、下载中心与反馈系统等。

**整体评价**：代码结构在 2026-06 的大重构后已较为清晰，且针对 Render 免费实例的**内存瓶颈**（Unity 任务串行锁、上传大小限制、临时目录清理）已有意识上的防护。但作为「长期运行在 Render 免费套餐上的个人工具站」，在**部署规范化、资源配额管理、可观测性与部分配置一致性**方面仍有明显改进空间。

| 维度 | 评分（1–5） | 说明 |
|------|------------|------|
| 架构清晰度 | 4 | Blueprint 拆分合理，共享模块已抽取 |
| Render 适配度 | 3 | 有自唤醒与内存锁，但缺标准部署配置与健康检查 |
| 安全性 | 3 | 有 IP 封禁与限流，但限流存储与上传限制存在漏洞 |
| 可维护性 | 2 | 缺 README、环境变量模板、版本锁定与测试 |
| 性能/成本 | 3 | 自唤醒策略消耗免费额度；存在未使用的大体积数据文件 |

**建议优先处理的 5 项**（详见下文 P0）：

1. 补齐 Render 部署配置（`render.yaml` + Gunicorn 单 worker）
2. 修复 `MAX_CONTENT_LENGTH`（20MB）与 Unity 路由声明上限（140MB）的矛盾
3. 新增 `/health` 健康检查端点（安全模块已白名单但路由未实现）
4. 清理或迁移 `data/card_data_1*`（约 10MB 无用资产，影响构建与冷启动）
5. 重新评估自唤醒（APScheduler）策略对 750 免费实例小时的影响

---

## 二、项目概况

### 2.1 技术栈

| 层级 | 技术 |
|------|------|
| 后端框架 | Flask |
| 进程模型 | Gunicorn（依赖已声明，但仓库内无启动命令配置） |
| 数据库 | Supabase（反馈 `feedbacks`、安全日志 `security_logs`） |
| 重型计算 | UnityPy、Pillow |
| 前端 | Vue 3 + Tailwind CSS（CDN 引入） |
| 定时任务 | Flask-APScheduler（进程内自唤醒） |
| 限流 | Flask-Limiter（`memory://` 存储） |

### 2.2 目录结构（核心）

```
MyProject/
├── app.py                 # 应用入口、蓝图注册、自唤醒调度
├── config.py              # 环境变量与上传限制
├── security.py            # IP 封禁、影子封禁、安全日志
├── extensions.py          # 全局限流
├── database.py            # Supabase 懒加载
├── logic_*.py             # 业务逻辑（Unity、EA API、关卡编辑等）
├── blueprints/            # 9 个功能蓝图
├── data/                  # 静态数据与 Unity 底包（约 11 MB）
├── static/                # Phantom 前端资源
└── templates/             # Jinja2 页面模板
```

### 2.3 功能模块与路由

| 蓝图 | 路径前缀 | 核心能力 |
|------|----------|----------|
| home | `/`, `/tools` | 首页、公告、更新日志 |
| deck_editor | `/deck-editor`, `/api/init_data`, `/api/quick_export` | 卡组编辑与一键打包 |
| unity | `/unity`, `/unpack`, `/repack`, `/unity/inspect` | AB 解包/回填/分析 |
| level_editor | `/editor`, `/api/editor/*` | 关卡编辑与 AB 打包下载 |
| phantom | `/phantom`, `/api/phantom/*` | 幻影卡牌 JSON 工坊 |
| card_sender | `/card-sender`, `/api/send-cards` | 卡牌发送（代理 EA API） |
| pack_buyer | `/pack-buyer`, `/api/buy-pack` | 卡包购买（代理 EA API） |
| downloads | `/downloads`, `/api/download/*` | 下载中心 |
| feedback | `/feedback`, `/api/feedback/submit` | 用户反馈（写入 Supabase） |

额外路由：`/security/stats`（需 `SECURITY_ADMIN_TOKEN`）

### 2.4 数据资产清单

| 文件 | 大小（约） | 是否被代码引用 |
|------|-----------|----------------|
| `data/card_data_1.json` | 7.37 MB | **否** — 疑似历史遗留 |
| `data/card_data_1` | 2.99 MB | **否** — 疑似历史遗留 |
| `data/index.json` | 0.10 MB | 是 — 全站卡牌索引 |
| `data/data_assets_36` | 0.44 MB | 是 — 关卡编辑器底包 |
| `data/recipe_decks_1` | 0.02 MB | 是 — 卡组工坊底包 |
| `data/recipe_definitions_1` | 0.02 MB | 是 — 卡组工坊底包 |
| 其余 JSON/TXT | < 0.1 MB | 是 |

**仓库总体积**（不含 `.git`）：约 **12.5 MB**，其中 `data/` 占 **11.1 MB**，近 90% 为数据文件。

---

## 三、Render 免费套餐约束基线

以下为本项目评估所依据的 Render 免费 Web Service 限制（摘自 [Render 官方文档](https://render.com/docs/free)）：

| 约束项 | 免费套餐行为 | 对本项目的含义 |
|--------|-------------|----------------|
| 空闲休眠 | **15 分钟**无入站流量后自动 Spin Down | 冷启动约 30–60 秒，用户已在公告中说明 |
| 本地文件系统 | 重启/休眠/重新部署后**全部丢失** | 不可用本地 SQLite；`out/` 临时输出仅限单次请求 |
| 实例时长 | 每月 **750 小时**（仅运行中计费，休眠不计） | 自唤醒会持续消耗额度 |
| 内存 | 约 **512 MB**（免费实例） | UnityPy 解包是主要 OOM 风险点 |
| 出站流量 | 计入月度带宽配额 | 大文件下载、EA API 代理均消耗带宽 |
| 服务主动出站 | 异常高出站流量可能被**暂停服务** | 自 ping + Supabase + EA API 均需控制频率 |
| 持久化磁盘 | **不支持** | 必须依赖 Supabase 或外部对象存储 |
| 扩展/SSH | 不支持 | 只能单实例排障，依赖日志 |
| `robots.txt` | 休眠时 Render 自动返回 Disallow | 不影响功能，但 SEO 几乎为零 |

---

## 四、现状优势（值得保留）

### 4.1 架构

- **Blueprint 模块化**：9 个蓝图职责清晰，2026-06 重构后删除了账号/社区等废弃模块。
- **共享逻辑抽取**：`logic_ea_api.py`、`utils/card_index.py`、`utils/json_data.py` 减少了重复代码。
- **Supabase 懒加载**（`database.py`）：避免启动阶段因 pydantic/supabase 链失败导致整个应用无法启动。

### 4.2 Render 免费实例适配（已有）

- **Unity 任务串行锁**（`UNITY_TASK_LOCK`）：防止并发解包打爆 512MB 内存，设计正确。
- **上传大小分层限制**：Bundle 140MB / 补丁包 90MB 的业务层校验（`unity.py`）。
- **临时目录清理**：`cleanup_old_temp()` + `finally` 中 `shutil.rmtree` + `gc.collect()`。
- **用户预期管理**：首页公告、赞助弹窗已说明 Render 免费部署与冷启动延迟。

### 4.3 安全

- **IP 封禁 + 影子封禁**：对反馈类接口返回假成功，降低攻击者探测价值。
- **环境变量扩展封禁列表**：`SECURITY_BLOCKED_IPS` 支持线上热更新。
- **部分接口限流**：反馈 3/h、发卡/购包 5/min、下载 5/min。
- **管理接口鉴权**：`/security/stats` 需 `X-Admin-Token`。

---

## 五、问题与风险分析

### P0 — 应立即处理

#### 5.1 缺少标准化 Render 部署配置

**现状**：

- `requirements.txt` 含 `gunicorn`，但仓库内**无** `render.yaml`、`Procfile`、启动命令说明。
- 无 `runtime.txt` 或 `.python-version` 锁定 Python 版本（本地 `__pycache__` 显示 3.14，与 Render 默认版本可能不一致）。
- 依赖**未锁定版本**（仅 `UnityPy>=1.20.26`、`pydantic` 有范围），复现构建风险高。

**风险**：换机器/重新创建 Render 服务时配置丢失；Gunicorn 默认多 worker 可能导致内存翻倍 OOM。

**建议**：

```yaml
# render.yaml（建议新增）
services:
  - type: web
    name: pvz-h-tools
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 2 --timeout 120 --max-requests 50 --max-requests-jitter 10
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.3"
```

同时新增 `.env.example` 列出所有环境变量（见附录 B）。

---

#### 5.2 上传大小配置自相矛盾

**现状**：

- `config.py`：`MAX_CONTENT_LENGTH = 20 * 1024 * 1024`（20 MB）
- `unity.py`：`MAX_BUNDLE_SIZE = 140 MB`，`MAX_PATCH_ZIP_SIZE = 90 MB`

Flask 会在请求进入视图**之前**拒绝超过 20 MB 的 body，Unity 路由中的 140MB 校验**永远不会被执行到**。

**风险**：用户无法按设计上传较大 Bundle，与 UI/文案承诺不符。

**建议**：

- 将 `MAX_CONTENT_LENGTH` 提升至与 Unity 路由一致（如 150MB），或
- 按路由拆分：仅 Unity 蓝图放宽限制（通过 `app.config` 动态调整或独立子应用），其余路由保持 20MB。

---

#### 5.3 `/health` 端点缺失

**现状**：`security.py` 将 `/health` 列入 `EXCLUDED_EXACT_PATHS`，但全项目**无对应路由实现**。

**风险**：

- 无法配置 Render Health Check Path，故障只能靠用户反馈发现。
- 外部监控（UptimeRobot 等）只能 ping `/`，触发完整页面渲染，浪费 CPU。

**建议**：

```python
@app.route("/health")
def health():
    return {"status": "ok"}, 200
```

在 Render Dashboard 中将 Health Check Path 设为 `/health`。

---

#### 5.4 未使用的 10MB 数据文件

**现状**：`data/card_data_1.json`（7.37MB）与 `data/card_data_1`（2.99MB）在代码中**零引用**。

**风险**：

- 每次 Git 拉取、Render 构建、容器镜像层都携带无用体积。
- 增加冷启动时磁盘 IO（虽不一定加载进内存，但影响部署速度）。

**建议**：确认无外部依赖后删除，或移至 Release Assets / 对象存储按需下载。

---

#### 5.5 自唤醒策略与免费额度

**现状**（`app.py`）：

- APScheduler 每 **14 分钟**向自身 URL 发 GET。
- 仅在北京时间 **08:00–24:00** 执行。
- 使用进程内 `requests` 出站流量。

**粗算**：

- 活跃时段约 16 小时/天 ×（60/14）≈ **68 次/天**
- 若每次唤醒使实例保持运行，月消耗可达 **数百小时**，逼近 750 小时上限（具体取决于用户真实访问叠加）

**风险**：

- 月底服务被 Render **暂停**（额度耗尽）。
- 属于 Render 文档中的「服务主动出站流量」，异常时可能被限制。
- 进程内 Scheduler 增加应用复杂度；多 worker 时可能重复 ping（若未注意 Gunicorn 配置）。

**建议（三选一或组合）**：

| 方案 | 优点 | 缺点 |
|------|------|------|
| A. 接受冷启动，移除自唤醒 | 省额度、架构更简单 | 用户体验下降 |
| B. 外部免费 Cron（如 cron-job.org）每 14 分钟 ping | 与应用进程解耦 | 依赖第三方 |
| C. 仅在前端展示「唤醒中」提示，用 UptimeRobot 低频 ping | 成本可控 | 仍有少量额度消耗 |

**推荐**：方案 B + 移除 APScheduler 依赖，可删掉 `flask-apscheduler` 与 `app.py` 中 27 行唤醒逻辑。

---

### P1 — 短期内应改进

#### 5.6 Flask-Limiter 使用内存存储

**现状**：`storage_uri="memory://"`

**风险**：Render 休眠/重启后限流计数器清零；无法防御持久化刷接口攻击。

**建议**：

- 免费方案：对重型接口（Unity 解包、quick_export、level pack）补充**应用层令牌桶**（文件锁或 Supabase 计数）。
- 若未来升级：Render Key Value（免费版重启丢数据）或 Upstash Redis 免费层。

---

#### 5.7 重型 API 缺少限流

**现状**：以下高成本接口**无限流**：

| 接口 | 风险 |
|------|------|
| `GET /api/init_data` | 每次请求触发 UnityPy 解析底包 |
| `POST /api/quick_export` | 内存打包 ZIP |
| `POST /unpack`, `/repack`, `/unity/inspect` | CPU/内存密集 |
| `POST /api/editor/ab/pack` | AB 重打包 |
| `GET /api/phantom/config` | 大 JSON 响应 |

**建议**：按接口添加 `@limiter.limit(...)`，例如 Unity 类 `3 per hour`，init-data `30 per hour`。

---

#### 5.8 Gunicorn 超时与 Unity 长任务

**现状**：无 `--timeout` 配置；Unity 解包大 Bundle 可能超过默认 30 秒。

**风险**：Worker 被 Gunicorn 强杀，用户看到 502，临时文件可能未清理。

**建议**：`--timeout 120` 或更长；必要时对超大任务返回「请使用本地版」。

---

#### 5.9 前端 CDN 依赖无降级

**现状**（`templates/base.html`）：

- `cdn.tailwindcss.com`
- `unpkg.com/vue@3`
- `fonts.googleapis.com`

**风险**：CDN 被墙或故障时整站 UI/交互瘫痪；无 SRI（子资源完整性）校验。

**建议**：

- 将 Vue / Tailwind 构建产物放入 `static/`（可用 Tailwind CLI 预编译常用类）。
- 添加 `integrity` + `crossorigin` 属性。
- 字体可 subset 后自托管。

---

#### 5.10 文档与 onboarding 缺失

**现状**：

- 无 `README.md`、`.env.example`。
- `news.json` 变更日志提到 `结构.md`，但仓库中**不存在**该文件。

**风险**：未来自己或协作者难以重建部署；与 changelog 描述不一致。

**建议**：至少补齐 README（本地运行、Render 部署、环境变量、Supabase 表结构）。

---

#### 5.11 `level_editor` 写入 `out/` 目录

**现状**：`logic_level_editor.py` 在项目根下 `out/data_assets_36` 写文件后 `send_file`。

**风险**：虽单次请求可工作，但长期运行会堆积垃圾文件（虽在 ephemeral FS 上最终会随重启消失，但在单次运行周期内仍占磁盘）。

**建议**：改为 `tempfile.mkdtemp` + 请求结束后删除，与 `unity.py` 模式统一。

---

### P2 — 中长期优化

#### 5.12 启动期同步加载

**现状**：

- `logic_data.py` 模块导入时即执行 `DataManager()` 单例，读取多个数据文件。
- `deck_editor` 的 `/api/init_data` 每次调用 `unity_processor.extract_all_to_memory()` 重新 UnityPy 解析。

**建议**：

- 启动时预加载并缓存 `extract_all_to_memory()` 结果（带文件 mtime 失效机制）。
- `index.json` 等大 JSON 使用 `functools.lru_cache` 或模块级缓存。

---

#### 5.13 错误监控与日志

**现状**：仅 `print` / `logging.error`，无 Sentry 等聚合。

**建议**：免费层可接入 Sentry Developer 计划或 Better Stack 免费档，至少捕获 500 错误与 Unity OOM。

---

#### 5.14 测试与 CI

**现状**：无单元测试、无 GitHub Actions。

**建议**：

- 为核心纯函数（`card_index`、`json_clean`、`pack_buyer` 解析）添加 pytest。
- CI 仅跑 lint + 测试，不部署。

---

#### 5.15 安全加固（可选）

| 项 | 说明 |
|----|------|
| CSRF | POST 表单/API 无 CSRF Token；纯 JSON API 风险相对较低，但仍可能被跨站调用 |
| `X-Forwarded-For` 伪造 | 未套 Cloudflare 时，IP 封禁可能被绕过；建议仅信任 Render 代理头 |
| 硬编码封禁 IP | `security.py` 中 `DEFAULT_BLOCKED_IPS` 宜全部迁移到环境变量 |
| EA Token 代理 | 服务端转发用户游戏 Token，需确保日志不记录完整 Token |

---

#### 5.16 带宽与构建优化

- `card_data_1.json` 删除后，项目体积可降约 **80%**。
- 考虑将 `index.json` 在构建阶段压缩为 `.json.gz`，API 返回时带 `Content-Encoding: gzip`（Flask-Compress）。
- 静态图片 PNG 可转 WebP 减小出站流量。

---

## 六、改进路线图

### 阶段 1：部署基线（1–2 天）

- [ ] 新增 `render.yaml` + `.env.example` + `README.md`
- [ ] 锁定 `requirements.txt` 版本（`pip freeze` 或 `pip-tools`）
- [ ] 实现 `/health` 并配置 Render Health Check
- [ ] Gunicorn：`--workers 1 --threads 2 --timeout 120`
- [ ] 修复 `MAX_CONTENT_LENGTH` 与 Unity 上限一致

### 阶段 2：成本与稳定性（3–5 天）

- [ ] 移除 APScheduler，改用外部 Cron ping（或接受冷启动）
- [ ] 删除无用 `card_data_1*` 数据文件
- [ ] 为重型 API 添加限流
- [ ] `level_editor` 临时文件改 `tempfile` 模式
- [ ] 缓存 `/api/init_data` 的 Unity 解析结果

### 阶段 3：体验与工程化（1–2 周）

- [ ] 前端 CDN 资源自托管 + SRI
- [ ] 补充 `结构.md` 或与 changelog 对齐
- [ ] pytest 核心模块 + GitHub Actions
- [ ] 可选：Sentry 错误监控

---

## 七、Render 部署检查清单

部署或迁移服务时，按此清单逐项确认：

| # | 检查项 | 状态 |
|---|--------|------|
| 1 | Render 服务类型为 **Web Service / Free** | 待确认 |
| 2 | Build Command：`pip install -r requirements.txt` | 待确认 |
| 3 | Start Command：Gunicorn 单 worker（见 5.1） | 待确认 |
| 4 | 环境变量：`SUPABASE_URL`、`SUPABASE_KEY` | 待确认 |
| 5 | 可选：`SECURITY_ADMIN_TOKEN`、`SECURITY_BLOCKED_IPS` | 待确认 |
| 6 | 可选：`PVZH_CLIENT_VERSION`、`PVZH_CONTENT_VERSION` | 待确认 |
| 7 | Health Check Path：`/health` | **未实现** |
| 8 | Supabase 表：`feedbacks`、`security_logs` 已建 | 待确认 |
| 9 | 自定义域名 + HTTPS（可选） | 待确认 |
| 10 | 监控：外部 Uptime ping（14 min 间隔） | 部分实现（内置 Scheduler） |

---

## 八、附录

### 附录 A：环境变量一览

| 变量名 | 必填 | 用途 |
|--------|------|------|
| `SUPABASE_URL` | 是 | Supabase 项目 URL |
| `SUPABASE_KEY` | 是 | Supabase anon/service key |
| `PORT` | 否 | Render 自动注入 |
| `SECURITY_ADMIN_TOKEN` | 否 | `/security/stats` 管理令牌 |
| `SECURITY_BLOCKED_IPS` | 否 | 逗号分隔的额外封禁 IP |
| `PVZH_CLIENT_ID` | 否 | EA API Client ID（有默认值） |
| `PVZH_CLIENT_VERSION` | 否 | 游戏客户端版本 |
| `PVZH_CONTENT_VERSION` | 否 | 内容版本哈希 |
| `PVZH_PLATFORM` | 否 | 默认 `Android` |
| `PVZH_REQUEST_TIMEOUT` | 否 | EA API 超时秒数，默认 12 |

### 附录 B：依赖清单与 Render 相关性

| 包 | Render 环境影响 |
|----|----------------|
| `UnityPy` | 内存/CPU 密集，是 OOM 主因 |
| `Pillow` | 图像导出时内存占用 |
| `gunicorn` | 必须使用单 worker |
| `flask-apscheduler` | 建议移除，改外部 Cron |
| `Flask-Limiter` | 内存存储，休眠后失效 |
| `supabase` | 出站 API 调用，计入带宽 |
| `requests` | 自唤醒 + EA 代理 |

### 附录 C：本次静态分析验证

- `python -c "import app"` — **通过**（应用可正常导入，9 个蓝图已注册）
- 项目体积统计 — **通过**（总计约 12.5 MB）
- 路由/限流/配置 — 代码审查完成

---

## 九、结论

本项目已经具备「个人 hobby 工具站」的良好基础：模块化清晰、对 Unity 内存风险有认知、安全模块有实际运营价值。若以 **Render 免费套餐长期稳定运行** 为目标，最紧迫的不是加新功能，而是：

1. **把部署变成可复现的代码**（render.yaml、gunicorn 参数、Python 版本）；
2. **把资源消耗对齐免费额度**（自唤醒策略、删除无用大文件、重型 API 限流）；
3. **把运维盲点补上**（health check、README、环境变量文档）。

完成阶段 1 后，项目可达到「免费套餐下的可维护生产 hobby 级」标准；阶段 2–3 则进一步提升稳定性与开发效率。

---

*文档由代码静态分析生成，未包含 Render Dashboard 线上配置与 Supabase 表结构的实地核查。部署相关「待确认」项建议在 Render / Supabase 控制台逐项核对。*